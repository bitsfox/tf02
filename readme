我三台机器所使用的conky获取系统信息的方式各不相同，打算整合一下整合为
一个实现文件。
不同机器的区分使用dmidecode -t 1 取得的系统信息来判断
单位本本的Version: Lenovo B470

2017-3-14 在这个项目中又添加了i3信息显示程序的集成子项目：i3msg
对于i3显示程序的集成，相比conky下的做法又更进了一步：
通过对#ifdef的使用将适用不同机器的代码分别编译，不采用运行时判断的
做法。
-------2017-7-30 下列的更新是为了适应debian9的变更----------
一、由于debian9下的conky配置文件位置发生了变化，随之也调整了天气获取程序所在的路径。
因此天气程序的修改一并放入该目录中。
二、由于debian9下的一些系统信息的存储位置不再像之前版本那样是固定的（温度，风扇转数等），
所以准备建立一个新的程序，用于在系统启动时将这些有变化的信息文件确定后保存在/tmp下的临时
文件中，这样需要时可直接取得所需的文件路径。减少conky或i3msg的资源消耗。该临时文件名称为：
/tmp/sysini_path0XAdb234WA99u.dtt
这个获取程序的代码也放置在本目录中。




